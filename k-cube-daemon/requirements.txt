# k-cube-daemon/requirements.txt

# 用于监控文件系统
watchdog

# 用于创建跨平台的系统托盘图标
pystray

# pystray 需要 pillow 来处理图像
Pillow

# 我们需要复用 k-cube 的核心逻辑
# 注意：这里我们假设 k-cube CLI 已经通过 pip install -e . 安装在同一个虚拟环境中
k-cube